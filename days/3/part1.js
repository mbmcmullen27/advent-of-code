const data = require('fs')
    .readFileSync('./input', 'utf8')
    .split(/\n/)

var zeroes;
var ones;

for (var i = 0; zeroes <= data.length/2 || ones <= length/2; i++) {
    
}

console.log(data.length)
console.log(data[0].split('').map((d)=>1 -d).join(''))

// for( const value of Buffer.from(data).values() ) {
    // console.log(value);
// }